feat: auto-generate SVG placeholder images for products without images, notify before import

- Deleted Shengadanyachi_Chikki product from DB
- New server/generatePlaceholder.js — SVG placeholder generator (no deps)
- Import-excel endpoint generates placeholders for products without images
- ImportExcel.jsx parses Excel client-side, shows confirm dialog if images missing
- Shows placeholder count in import result summary
